# Quiz Game — Система тестирования и викторин

##  Обзор проекта

Консольное приложение-викторина, демонстрирующее гибкую систему вопросов с поддержкой различных типов ответов. Проект показывает, как структурировать данные, обрабатывать пользовательский ввод и создавать расширяемую архитектуру для тестирующих систем.

##  Функциональность

- **Два типа вопросов** — текстовые и числовые ответы
- **Множество правильных ответов** — синонимы, альтернативные написания
- **Автоматическая обработка ввода** — приведение к нижнему регистру, удаление пробелов
- **Система подсчёта очков** — накопление баллов за правильные ответы
- **Информативная обратная связь** — показ правильного ответа при ошибке

##  Технологии

- Python 3.x
- Структурированные данные (список словарей)
- Динамическая типизация и обработка типов
- Консольный ввод/вывод

##  Структура проекта

```
quizz-game/
│
├── quizz.py          # Основной файл программы
└── README.md        # Документация
```

##  Детальный разбор кода

### 1. Функция обработки вопроса

```python
def ask_question(question_data):
    """Задаёт вопрос и проверяет ответ"""
    user_answer = input(question_data["text"])
    
    # Обработка ответа в зависимости от типа
    if question_data["type"] == "number":
        user_answer = int(user_answer)
    else:
        user_answer = user_answer.lower().strip()
    
    # Проверка (поддерживает несколько вариантов ответа)
    correct_answers = question_data["answers"]
    if user_answer in correct_answers:
        print(" Правильно!")
        return 1
    else:
        print(f" Неправильно! Правильный ответ: {correct_answers[0]}")
        return 0
```

#### 1.1 Архитектура функции

| Компонент | Назначение |
|-----------|------------|
| **Входные данные** | Словарь с ключами `text`, `answers`, `type` |
| **Обработка** | Конвертация типа, нормализация строк |
| **Проверка** | Поиск в списке допустимых ответов |
| **Выходные данные** | 1 (верно) или 0 (неверно) |

#### 1.2 Адаптивная обработка типов

**Для числовых ответов:**
```python
user_answer = int(user_answer)  # "7" → 7
```
- Преобразование строки в целое число
- Сравнение с числами в списке `answers`

**Для текстовых ответов:**
```python
user_answer = user_answer.lower().strip()  # " ПАРИЖ " → "париж"
```
- Приведение к нижнему регистру
- Удаление лишних пробелов
- Сравнение без учёта регистра и форматирования

#### 1.3 Механизм проверки

```python
if user_answer in correct_answers:
    return 1  # Победа!
else:
    print(f"Правильный ответ: {correct_answers[0]}")  # Подсказка
    return 0  # Поражение
```

**Особенности:**
- Поддержка множества правильных ответов
- Показ **первого** варианта как эталонного
- Накопление баллов через возвращаемые значения

---

### 2. Структура данных вопросов

```python
questions = [
    {
        "text": "Столица Франции? ",
        "answers": ["париж"],
        "type": "text"
    },
    {
        "text": "Сколько дней в неделе? (число) ",
        "answers": [7],
        "type": "number"
    },
    {
        "text": "Какой язык программирования больше всего используем? ",
        "answers": ["python", "питон", "пайтон"],
        "type": "text"
    },
    # ... ещё 7 вопросов
]
```

#### 2.1 Схема данных

```
┌─────────────────────────────────────┐
│           Словарь вопроса           │
├─────────────┬───────────────────────┤
│ "text"      │ Строка с вопросом     │
├─────────────┼───────────────────────┤
│ "answers"   │ Список ответов (list) │
├─────────────┼───────────────────────┤
│ "type"      │ "text" / "number"     │
└─────────────┴───────────────────────┘
```

#### 2.2 Категоризация вопросов

**Текстовые вопросы (8 шт.):**
- География: столицы, страны, водопады
- Общие знания: языки программирования
- Культура: достопримечательности

**Числовые вопросы (3 шт.):**
- Математика: арифметика
- Календарь: дни недели, текущий год
- Вычисления: умножение больших чисел

---

### 3. Игровой движок

```python
# Основная игра
score = 0
for question in questions:
    score += ask_question(question)

# Результат
print(f"\n🏆 Ваш счёт: {score} из {len(questions)}")
print("📚 Спасибо за игру!")
```

#### 3.1 Процесс игры

```
Старт
  │
  ▼
score = 0
  │
  ▼
┌──────────────────────┐
│ Цикл по вопросам     │◄────┐
│ for q in questions:  │     │
└──────────┬───────────┘     │
           ▼                 │
    ┌──────────────┐         │
    │ ask_question │         │
    └──────┬───────┘         │
           ▼                 │
    score += результат       │
           │                 │
           └─────────────────┘
  │
  ▼
Вывод финального счёта
```

#### 3.2 Подсчёт результатов

- **Максимум:** `len(questions)` = 10 баллов
- **Минимум:** 0 баллов
- **Формула:** `score = сумма правильных ответов`

---

## 🔧 Установка и запуск

1. Создайте файл `quiz.py`
2. Скопируйте код в файл
3. Запустите викторину:

```bash
python quiz.py
```

---

## 📊 Пример игровой сессии

```
Столица Франции? Париж
 Правильно!

Сколько дней в неделе? (число) 7
 Правильно!

Какой язык программирования больше всего используем? питон
 Правильно!

Какая планета ближе всего к Солнцу? меркурий
 Правильно!

...

 Ваш счёт: 8 из 10
 Спасибо за игру!
```

---

## 🎓 Изученные концепции Python

| Концепция | Применение в проекте |
|-----------|---------------------|
| **Список словарей** | Хранение структурированных данных вопросов |
| **Условные операторы** | Ветвление по типу вопроса |
| **Преобразование типов** | `int()` для числовых ответов |
| **Методы строк** | `.lower()`, `.strip()` для нормализации |
| **Оператор in** | Проверка наличия в списке ответов |
| **Цикл for** | Перебор всех вопросов |
| **Аккумулятор** | `score += ...` для подсчёта очков |
| **Индексация** | `correct_answers[0]` — первый правильный ответ |

---

## 🧠 Архитектурные паттерны

### 1. Разделение данных и логики

```
┌──────────────────┐     ┌──────────────────┐
│                  │     │                  │
│   ДАННЫЕ         │────▶│   ЛОГИКА         │
│   (questions)    │     │   (ask_question) │
│                  │     │                  │
└──────────────────┘     └──────────────────┘
```

**Преимущества:**
- Легко добавлять новые вопросы без изменения кода
- Можно загружать вопросы из JSON/файлов
- Упрощается тестирование

### 2. Полиморфизм через тип

```python
if type == "number":
    # обработка числа
else:  # text
    # обработка строки
```

**Гибкость:** Добавление нового типа требует лишь одного `elif`

---

##  Возможные улучшения

###  Расширение типов вопросов

- [ ] **Да/Нет** — булевы значения
- [ ] **Множественный выбор** — несколько правильных ответов
- [ ] **Регулярные выражения** — сложные паттерны ответов
- [ ] **Даты** — работа с `datetime`

###  Система сложности

- [ ] **Уровни вопросов** — лёгкие, средние, сложные
- [ ] **Разные баллы** — сложные вопросы приносят больше очков
- [ ] **Штрафы** — за неправильные ответы

###  Статистика

- [ ] **Рекорды** — лучшие результаты
- [ ] **Время** — ограничение по времени на вопрос
- [ ] **Прогресс** — отслеживание улучшений
---

##  Примеры расширения

### Добавление вопроса с множественным выбором:

```python
{
    "text": "Какие языки являются компилируемыми? (введите через запятую) ",
    "answers": ["c", "c++", "rust", "go"],
    "type": "multiple",
    "min_correct": 2  # минимум правильных для зачёта
}
```

---

## 🎮 Варианты использования

1. **Образование** — проверка знаний студентов
2. **Собеседования** — технические викторины
3. **Развлечение** — игры с друзьями
4. **Самообучение** — создание карточек для запоминания

---

##  Анализ вопросов

| Вопрос | Тип | Сложность | Интересные особенности |
|--------|-----|-----------|----------------------|
| Столица Франции | Текст | Легко | Классический вопрос |
| Дни в неделе | Число | Легко | Проверка базовых знаний |
| Язык программирования | Текст | Средне | 3 варианта ответа |
| Планета к Солнцу | Текст | Легко | Астрономия |
| Самая большая страна | Текст | Легко | География |
| 2+2*2 | Число | Средне | Приоритет операций |
| Текущий год | Число | Легко | Актуальность |
| Самая маленькая страна | Текст | Сложно | Эрудиция |
| 10*900000 | Число | Легко | Умножение |
| Водопад Анхель | Текст | Сложно | Редкие знания |

---

##  Технические детали

### Обработка граничных случаев

```python
# Что если пользователь ввёл не число в числовом вопросе?
try:
    user_answer = int(user_answer)
except ValueError:
    print(" Ошибка! Введите число.")
    return 0
```

**Текущий код:** Вызовет `ValueError` и аварийное завершение

**Улучшение:** Добавить `try-except` для безопасности

```python
# Текущий подход:
score += ask_question(q)  # 0 или 1

# Альтернатива:
if ask_question(q):
    score += 1
```

**Текущий подход** — компактнее и элегантнее

**Что сделано хорошо:**
- ✅ Чистая архитектура с разделением данных и логики
- ✅ Гибкая система проверки ответов
- ✅ Поддержка нескольких правильных вариантов
- ✅ Понятный интерфейс
- ✅ Масштабируемая структура данных

**Что можно улучшить:**
- ⬜ Обработка ошибок ввода
- ⬜ Случайный порядок вопросов
- ⬜ Сохранение результатов
- ⬜ Категории вопросов
- ⬜ Пропуск вопроса

*Создано с ❤️ для изучения структур данных и логики проверки ответов в Python*
